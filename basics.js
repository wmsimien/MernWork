//Q1. Create a file with name basics and show all the features that you know about javascript

/**
 * Javascript it a scripting language; it executes the code line by line instead of compiling the 
 * code to machine code first; basically, it executes code on the fly.
 * 
 * Javascript is dymamically typed, that is since variable are untype and can be assigned any value,
 * durng runtime Javascript will automatcially change/set the data type.
 * 
 * Javascript can be used on the client side and server side (help of Node.js)
 * 
 * 
 */
